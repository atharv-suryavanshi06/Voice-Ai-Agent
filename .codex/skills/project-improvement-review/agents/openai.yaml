interface:
  display_name: "Project Improvement Review"
  short_description: "Evidence-backed whole-repository improvement review"
  default_prompt: "Use $project-improvement-review to analyze this repository and produce a prioritized, evidence-backed improvement plan."

dependencies:
  tools:
    - type: "mcp"
      value: "codebase_memory"
      description: "Indexes and queries the repository knowledge graph"
